# Server

PORT = 3000
NODE_ENV = development
LOG_LEVEL = info

# Database

DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=

# Arcjet

ARCJET_KEY =

# Supabase
# Use the NEW API keys (sb_publishable_... / sb_secret_...) from
# Project Settings → API Keys. Legacy anon/service_role keys still work
# but are deprecated for new projects.

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=